# Book page blueprint — retired

Status: historical route blueprint; not current product guidance.

The former `/book` marketing/booking page body is retired. `/book` is retained only as a compatibility entrypoint and redirects to the canonical `/contact` route.

Current Contact implementation and messaging live in:

- `src/app/(marketing)/contact/**`
- `src/components/public/PublicEditorialHero.tsx`
- `docs/website-copy-blueprint.md` where current copy constraints apply

Do not restore a separate booking page, BuildFlow credibility section, Kits/product sales flow, or legacy booking funnel from earlier revisions of this document.

If a future scheduling/booking product is approved, design it as a new current requirement against the canonical product strategy rather than reviving the retired `/book` body.

Historical implementation details remain available in Git history.
