# ProChat Public Platform Handoff — 2026-07-15

## Purpose

This document is the restart point for a new conversation. It captures the verified repository state, completed work, current blockers, exact uncommitted paths, and the next bounded packet.

## Source lock

```yaml
sourceId: prochat
repository: ProChat public platform
```

Mind remains canonical for company, product, brand, and public-platform strategy. The ProChat repository is canonical for implementation, migration, validation, and execution state.

## Verified repository state

```yaml
verified_date: 2026-07-15
current_head: 784c69248ac768848346c66f07aeafcbfacb7a84
latest_commit: "docs(prochat): finalize ppf-001 runner decision"
tracked_worktree: clean
canonical_visual_routes_active: 0
production_visual_design_started: false
legacy_implementation_archived_count: 0
live_browser_execution: NOT_RUN
PPF-001: BLOCKED
PPF-002: NOT_READY
```

`git status --short` currently reports only the isolated browser-runner package:

```text
?? .github/workflows/wave1-browser-equivalence.yml
?? tests/
```

The intended uncommitted files under those paths are:

```text
.github/workflows/wave1-browser-equivalence.yml
tests/evidence/playwright.wave1.config.ts
tests/evidence/wave1-shell-equivalence.spec.ts
tests/evidence/validate-wave1-browser-runner.mjs
```

No production source, package file, lockfile, route, visual shell, archive content, or public page is currently modified.

## Active Workbench state

Workbench is connected to `sourceId prochat`.

The persistent run still recorded as active is:

```text
agent-c41afa81-19f4-415c-bbc6-66731c135a78
```

Its recorded scope is stale:

```yaml
goal: R1B documentation reconciliation
status: running
completed_packets: 0
active_task: task-1-requirements-roadmap
```

A new conversation must verify that no validation job is attached, then checkpoint or close this stale run before creating the next bounded PPF-001 run.

## Completed foundation work

### Mind commits

```text
1461678 — docs(prochat): establish public platform foundation
7687bb8 — docs(prochat): finalize public platform foundation
```

### ProChat foundation and migration commits

```text
8180a7d — docs(prochat): plan public platform foundation sweep
cb3924d — docs(prochat): checkpoint public platform foundation
301a5ec — docs(prochat): inventory public routes
85c85fc — docs(prochat): inventory component systems
5f21f73 — docs(prochat): inventory style and motion systems
31bc501 — docs(prochat): inventory assets and dependencies
854d03e — docs(prochat): classify foundation migration waves
dbdfc7a — chore(prochat): establish legacy archive boundary
20a55f9 — feat(prochat): add canonical foundation baseline
b3739ba — docs(prochat): plan canonical shell compatibility
4d12c05 — refactor(prochat): establish shell routing boundary
ce3df67 — docs(prochat): verify shell routing equivalence
377bc91 — docs(prochat): complete browser shell verification
220e106 — docs(prochat): provision browser shell verification
806ce439 — docs(prochat): reconcile execution roadmap
039d3bf — docs(prochat): record ppf-001 handoff state
15e4d8b — docs(prochat): reconcile ppf-001 browser decision
784c692 — docs(prochat): finalize ppf-001 runner decision
```

## Canonical company and product hierarchy

```text
Company: ProChat
Flagship product: ProChat Memory
First discipline-specific edition and primary initial conversion path: ProChat Memory for QA
Second product: ProChat Workbench
Founder: Steve Westhoek — QA Engineer and Founder of ProChat
```

The main public website is company-first and flagship-focused. It is not a freelance portfolio.

## Approved design foundation

```yaml
primary_font: Golos Text
technical_font: JetBrains Mono
secondary_font: null
palette: grayscale plus ProChat Cobalt
prochat_cobalt: "#3158C7"
default_public_mode: light
native_scroll: true
micro_interactions: CSS
cinematic_motion: GSAP + ScrollTrigger only after approval
product_visuals: semantic HTML + CSS + SVG
```

The additive canonical foundation exists but is not active on any current route:

```text
src/assets/styles/prochat-foundation.css
src/lib/prochat-fonts.ts
```

## Route and shell state

All 84 route records have executable shell classifications:

```yaml
canonical_public_shell: 9
protected_internal_shell: 16
temporary_legacy_compatibility: 24
no_shared_shell: 35
```

The current canonical visual allowlist is empty.

The shell contract is recorded as:

```yaml
shell_contract: DECIDED_WITH_FUTURE_REPAIR
```

`AppChrome` repair is future work and must not be combined with the runner packet.

## Archive and cleanup state

The archive boundary exists at:

```text
archive/legacy-public-platform/
```

It currently contains governance only. No existing production implementation has been moved into it.

Legacy BuildFlow and ProChat OS public material is approved for later archival/public retirement after replacements, redirects, and obligation checks.

No final destructive cleanup has started.

## Canonical planning documents

Read these first in a new conversation:

```text
docs/product/agent-mode-progress.md
docs/roadmap.md
docs/implementation-plan.md
docs/migration/PPF001_BROWSER_RUNNER_AND_SHELL_DECISION.md
docs/migration/WAVE1_BROWSER_EQUIVALENCE_REPORT.md
docs/migration/CONTENT_AUDIT.md
docs/migration/MIGRATION_WAVES.md
docs/migration/MIGRATION_MATRIX.md
```

Important current statuses:

```yaml
runner_disposition: MERGE_READY_REQUIRES_APPROVAL
static_validation: PASSED
live_execution: NOT_RUN
protected_session_strategy: PUBLIC_AND_UNAUTHENTICATED_ONLY
authenticated_protected_proof: SEPARATE_FUTURE_RUNNER
PPF-001: BLOCKED
PPF-001_blocker: explicit approval to commit protected workflow package
PPF-002: NOT_READY
content_audit: seeded but incomplete
```

## Browser-runner package state

The isolated runner package is structurally validated and intentionally uncommitted.

Files:

```text
.github/workflows/wave1-browser-equivalence.yml
tests/evidence/playwright.wave1.config.ts
tests/evidence/wave1-shell-equivalence.spec.ts
tests/evidence/validate-wave1-browser-runner.mjs
```

The runner is scoped to:

- public shell-equivalence verification;
- public and unauthenticated route rendering;
- unauthenticated protected-route behavior;
- health and OG endpoint evidence;
- five required viewports;
- fifteen route records;
- mobile-navigation proof;
- commit and browser provenance;
- blocked third-party request classification;
- screenshot comparison;
- no authenticated authorization or state-transition proof.

Validated before the documentation commit:

```text
YAML parsing: passed
TypeScript syntax: passed
node tests/evidence/validate-wave1-browser-runner.mjs: passed
git diff --check: passed
documentation-state consistency: passed
```

The workflow has not been dispatched. Playwright has not been run. No browser evidence exists yet.

## Immediate blocker

The only authorized next action is explicit approval and commit of the four runner files.

The workflow path is protected:

```text
.github/workflows/wave1-browser-equivalence.yml
```

Therefore, the runner package must not be committed without explicit user approval.

Proposed commit:

```text
test(prochat): add wave1 browser equivalence runner
```

## Exact next sequence

1. Verify Workbench connectivity, HEAD `784c69248ac768848346c66f07aeafcbfacb7a84`, and the four remaining runner paths.
2. Re-run YAML, TypeScript syntax, static validator, security scan, and `git diff --check`.
3. Review the exact four-file diff.
4. Obtain explicit approval for the protected workflow path.
5. Commit exactly the four runner files with `test(prochat): add wave1 browser equivalence runner`.
6. Confirm a clean worktree.
7. Checkpoint the PPF-001 run.
8. Prepare PPF-001G browser-execution preparation as a separate packet.
9. Do not dispatch the workflow until GitHub Environment, manual inputs, evidence-review procedure, and rollback rules are explicitly approved.
10. Do not begin PPF-002.

## Restrictions that remain in force

- Do not modify production source.
- Do not activate a canonical route.
- Do not begin the design lab.
- Do not install GSAP.
- Do not begin PPF-002.
- Do not move legacy files into the archive yet.
- Do not remove dependencies yet.
- Do not run protected authenticated flows in the public shell-equivalence runner.
- Do not execute the workflow before the runner commit and execution-preparation packet.

## Restart prompt

```text
Resume the ProChat Public Platform Foundation Program using sourceId prochat.

Read first:
- docs/product/HANDOFF_2026-07-15_PPF001.md
- docs/product/agent-mode-progress.md
- docs/roadmap.md
- docs/implementation-plan.md
- docs/migration/PPF001_BROWSER_RUNNER_AND_SHELL_DECISION.md

Current expected HEAD:
- 784c69248ac768848346c66f07aeafcbfacb7a84

Current expected uncommitted paths:
- .github/workflows/wave1-browser-equivalence.yml
- tests/evidence/playwright.wave1.config.ts
- tests/evidence/wave1-shell-equivalence.spec.ts
- tests/evidence/validate-wave1-browser-runner.mjs

First:
1. Confirm Workbench connectivity and lock sourceId prochat.
2. Verify exact HEAD, branch, Git status, and active run.
3. Confirm no unrelated changes exist.
4. Re-run YAML parsing, TypeScript syntax validation, the static runner validator, security scan, and git diff --check.
5. Review the exact four-file diff.

I explicitly approve committing these four paths:
- .github/workflows/wave1-browser-equivalence.yml
- tests/evidence/playwright.wave1.config.ts
- tests/evidence/wave1-shell-equivalence.spec.ts
- tests/evidence/validate-wave1-browser-runner.mjs

Commit exactly those four paths with:

test(prochat): add wave1 browser equivalence runner

After commit:
- confirm a clean worktree;
- checkpoint the active Workbench run;
- keep PPF-001 open until browser evidence is reviewed;
- keep PPF-002 NOT_READY;
- do not execute the workflow;
- return the exact next prompt for PPF-001G browser-execution preparation.
```



## PPF-001G browser-execution preparation checkpoint — 2026-07-16

Preparation only. The browser workflow was not executed, dispatched, or simulated.

- Source: `prochat`
- Branch: `main`
- HEAD: `08549b7eef8657586c0a4154d46fc87dc44a48ce`
- Active run: `agent-595f1bf7-63d1-4a13-899d-5cb3f6ba5189`
- Runner commit scope verified as exactly the workflow, Playwright config, shell-equivalence spec, and static validator
- Static runner validation passed: valid YAML, TypeScript syntax, 17 workflow steps, 15 route records, 5 viewports, pinned actions
- Secret-material scan passed with zero findings
- `git diff --check` passed
- Focused `tsc` could not resolve `@playwright/test` because it is intentionally absent from repository dependencies; no package installation or browser execution was performed
- Workflow dispatch inputs: `baseline_ref` full SHA, `target_ref` full SHA, `retain_days` integer string (default 14)
- GitHub Environment: `wave1-browser-verification`
- Permissions: `contents: read`
- Runner timeout: 90 minutes
- No concurrency group is defined; overlapping manual dispatches must be avoided
- Expected artifact: `wave1-browser-equivalence-<baseline_sha>-<target_sha>`
- Evidence paths: `harness/playwright-report/**`, `harness/test-results/**`, and `harness/tests/evidence/**/*-snapshots/**`
- Coverage: 15 route records across 320, 768, 1024, 1440, and 1728 pixel viewports
- `/chat/[projectID]` remains explicitly excluded for the separate protected runner
- Success requires exact provenance, healthy baseline and target services, passing Chromium checks, no unexpected console/request failures, passing route/viewport/accessibility/mobile-navigation/request-policy assertions, and produced evidence artifacts
- Failure triage: verify SHAs, database lifecycle, service startup, line reporter, HTML report, test-results, screenshots, traces, then classify by route, viewport, console, request, accessibility, mobile navigation, or canonical-scope assertion
- Cancellation: cancel the GitHub Actions run; cleanup kills recorded baseline and target processes; no deploy or push step exists
- PPF-001 remains open
- PPF-002 remains `NOT_READY`



## PPF-001G execution attempt — remote workflow blocker

- Date: 2026-07-16
- Source: `prochat`
- Branch: `main`
- Local HEAD: `08549b7eef8657586c0a4154d46fc87dc44a48ce`
- Worktree remained unchanged except for this untracked handoff
- Runner files were confirmed unchanged after commit `08549b7`
- GitHub CLI lookup for `.github/workflows/wave1-browser-equivalence.yml` returned HTTP 404 because the workflow is not present on the remote default branch
- Local branch state: `main...origin/main [ahead 21]`
- No workflow run was dispatched
- No duplicate or overlapping run was created
- No push was attempted because explicit push authorization was not provided
- PPF-001 remains open
- PPF-002 remains `NOT_READY`

Required next step: obtain explicit authorization to push the approved local branch state or otherwise publish commit `08549b7` and its prerequisite commits to the remote default branch before dispatching the workflow exactly once.
